# 验证记录

验证日期：2026-09-19。测试数据均为合成数据。

## 已执行

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm test` | 4 个文件、44 个离线测试通过；1 个可选真实 API 测试本次跳过（此前单独启用后通过） |
| `npx expo install --check` | 依赖与 SDK 57 兼容 |
| Android JS / Hermes bundle | 成功生成 |
| Android Gradle `assembleRelease` | 成功，arm64-v8a + x86_64，调试证书签名 |
| 安装与冷启动 | Android API 36 x86_64 模拟器通过；无需 Metro |
| 手动记录 | 保存 128/82、心率 73 成功 |
| 编辑 / 删除 | 修改为 129/82 后列表更新；删除前确认，确认后记录移除 |
| 本机持久化 | 强制停止后重新打开，记录和图表数据保留 |
| Excel 本机恢复 | 24 条中文合成记录成功预览、确认导入 |
| 图表统计 | 7 天 8 条记录（含手动记录），2 条血压异常；心率缺省未计为 0 |
| 图片报告 | 生成 1 张汇总及 2 张明细 PNG；保存到系统 Documents 目录成功 |
| 报告视觉检查 | 实际导出图片中文、图表、完整分页及页脚均正常，无截图截断 |
| 系统分享 | PNG 三图分享、XLSX 单文件分享均打开系统分享面板并提供文件预览；未发送给联系人 |
| 缺少 Key | AI 操作引导前往设置，普通离线功能不受影响 |
| Key 存储 | 保存后仅头尾显示，输入框清空；真实连接测试成功；联调后测试 Key 已从模拟器删除 |
| 图片 AI 全流程 | 从系统照片选择合成血压计图；正确识别 128/82、73；无日期时标为待补充，未直接入库 |
| AI 健康评估 | 此前版本已验证；当前版本按要求移除页面与入口 |
| 提醒配置 | 系统通知授权后成功注册 08:00 / 20:00 两个闹钟，由 `dumpsys alarm` 验证 |
| 源码密钥扫描 | 应用代码、脚本、配置和文档中未发现嵌入的测试 API Key |
| APK 检查 | 内嵌 Hermes bundle 未发现 API Key；签名验证通过，minSdk 24、targetSdk 36、arm64-v8a / x86_64 |

交付文件：`artifacts/pressure-journal-1.0.0.apk`，72,045,748 字节（约 68.7 MiB）。SHA-256：`F9C695977B493F2A816AA363A68CEBAEA8A8436BA416768D58F20796DA2C61EE`。

最新更新：记录页改为两列，左列日期 / 时分，右列血压 / 带心率图标的心率，保留异常颜色和箭头。趋势范围持久化到 SQLite，模拟器选择一年后强制停止并冷启动，仍显示一年。设置中的清空全部本地数据入口和操作处理已删除。本次类型检查及 release 构建通过，已安装模拟器；真机覆盖安装再次被拒绝（INSTALL_FAILED_ABORTED: User rejected permissions），尚未更新到这个版本。

界面精简与流程更新：删除页面宣传语、重复说明和 AI 评估入口；记录页使用表格及菜单，取消时间筛选；趋势使用下拉范围并增加一年，血压参考虚线按收缩压绿色、舒张压蓝色区分。模拟器验证选择 Excel 后停留待导入、点击开始后生成 24 条预览；重复项标记正常，点击预览行可打开编辑弹窗。记录表格、菜单导航、趋势一年选项与参考线已检查；录入页拍照 / 相册菜单和无 Key 引导已检查。新增一年闰日边界、备份离线批次及取消测试。图片识别复用原客户端，本次未重复调用真实 AI；新版拍照后自动填写和多条识别选择仍需真实图片端到端验收。本次 release 已安装模拟器；V2352A 覆盖安装被手机拒绝（INSTALL_FAILED_ABORTED: User rejected permissions），待手机允许后重试。

大字体布局更新：模拟器在 200% 字体及约 320dp 窄屏下检查首页，血压保持单行，心率、时间、状态与操作按钮自适应分行，未出现横向溢出。左右臂、服药情况改为选择框，验证选择右臂和服药后、保存并重新打开后保留。测试后已恢复模拟器字体和屏幕尺寸。本次 TypeScript 检查及 41 个离线测试通过，可选真实 AI 测试未重复执行；release 构建成功，已覆盖安装并启动当前连接的 V2352A，未清空数据或改变手机字体设置。

智能导入补充信息更新：图片和 Excel 均传递默认年份与说明，默认不填当前年份；非法年份和过长说明被拒绝。失败重试使用修正后的说明，已识别结果保持原样。真实 DeepSeek 请求验证了无年份记录补为 2025 年、明确的 2024 年保持不变、仅有月日而缺少时分的记录仍标为缺失时间。模拟器确认新增输入框正常显示，release APK 已重新构建。

## 自动化覆盖

两列表格版本安装重试：最新 APK（SHA-256 以 F9C69597 开头）已成功覆盖安装到 V2352A，启动检查通过。手机上的记录和设置未清空。

手机安装重试：用户允许安装后，最新 release APK 已通过 `adb install --user 0 -r` 成功覆盖安装到 V2352A；启动检查通过，未卸载应用或清空数据。

本次表格补充检查：200% 系统字体下，时间和血压保持完整，横向滚动可查看心率及状态；恢复模拟器字体为 100%。手机字体设置未修改。

- 家庭血压及危险阈值、心率空值、血压倒置、非整数校验。
- 7/30 天边界、未来记录过滤、半年月底夹取、本地时间和严格带时区日期解析。
- 日均值和范围、异常峰值保留、心率缺省统计。
- 等价时间重复识别、批次内重复、短/长密钥遮蔽。
- 四种导出范围的 Excel 往返，中文、毫秒时间、原始时区、可选心率及备注保留。
- Excel 公式注入防护、多工作表分批、Excel 日期、旧版 `.xls`。
- 长中文备注完整分页。
- AI 缺失时间、相册不补造日期、拍照时间待确认、非法 JSON 和结构缺失。
- 无 Key 不请求网络；401/402/413 错误；429 有界重试；取消及截断输出处理。

真实 API 独立冒烟测试另执行了 2 次请求：结构化文本（70 tokens）与合成图片（275 tokens），均成功。之后的应用内连接、图片识别和健康评估也使用了真实接口。没有在代码、APK 或文档内保存测试 Key。

## 待真机验收

- 更多品牌手机、Android 7–16 及其他字体和屏幕尺寸组合（模拟器 200% 字体、约 320dp 窄屏已检查）。
- 实际血压计反光、倾斜、低照度照片和相机权限拒绝/恢复。
- 微信上传、邮件附件等目标 App 对单/多文件分享的实际接收表现。
- 各厂商后台限制、重启、时区变化和省电模式下的提醒实际送达时间。
- 大型真实 Excel、复杂合并表头、手写记录；模型识别仍必须人工复核。
- 长时断网、低存储空间与后台进程被杀后的操作恢复。

已按用户请求在 V2463A、V2352A 上安装并启动 release APK；本次大字体布局和选择框更新安装到当前连接的 V2352A。没有发送文件给他人。安装启动及模拟器结果不能替代以上完整真机验收。

## 依赖审计

当前 `npm audit` 报告 14 项 moderate、0 项 high / critical，集中于 Expo 上游依赖链：`xcode → uuid` 和 `expo-router → query-string → decode-uri-component`。npm 建议的自动修复会把 Expo / Router 降到不兼容旧版本，因此未强制降级。正式发布前需重新审计并跟进兼容修复，尤其是深链接参数解码的拒绝服务风险。
